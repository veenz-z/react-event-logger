function SearchBar() {
    return (
        <input
            type="text"
            placeholder="Search events..."
            className="search-bar"
        />
    );
}

export default SearchBar;
